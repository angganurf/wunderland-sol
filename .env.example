# ============================================================
# WUNDERLAND ON SOL — Root Environment Variables
# ============================================================
#
# This file is primarily for server-side scripts (Anchor + TS scripts).
# For the Next.js frontend, use `app/.env.example` → `app/.env.local`.
#
# ============================================================

# Solana RPC endpoint (server-side scripts)
# Devnet: https://api.devnet.solana.com
# Mainnet: https://api.mainnet-beta.solana.com (or your RPC provider)
SOLANA_RPC=https://api.devnet.solana.com

# Optional: cluster hint for scripts (devnet | testnet | mainnet-beta)
SOLANA_CLUSTER=devnet

# Optional: path to Solana keypair JSON (used by scripts as the registrar / upgrade authority)
# Defaults to ~/.config/solana/id.json when present.
# SOLANA_KEYPAIR=/Users/you/.config/solana/id.json

# Optional signer for scripts (base58-encoded secret key or JSON array, depending on script)
# SOLANA_PRIVATE_KEY=<secret-key>

# Wunderland Sol Program ID (set after deployment)
# PROGRAM_ID=ExSiNgfPTSPew6kCqetyNcw8zWMo1hozULkZR1CSEq88

# Seeder override: explicit registrar keypair path (must match program upgrade authority for initialize_config)
# SEED_REGISTRAR_KEYPAIR=/Users/you/.config/solana/id.json

# Colosseum Hackathon (gitignored in .env.hackathon)
# COLOSSEUM_API_KEY=<agent-api-key>

# ============================================================
# submit-tip.ts CLI helper (scripts/submit-tip.ts)
# ============================================================
# Usage:
#   TIPPER_KEYPAIR_PATH=... PROGRAM_ID=... CONTENT_HASH_HEX=... TIP_AMOUNT_SOL=0.02 \
#     pnpm tsx scripts/submit-tip.ts
#
# Required:
# TIPPER_KEYPAIR_PATH=/abs/path/to/tipper.json
# CONTENT_HASH_HEX=<64-hex-chars-sha256>
# TIP_AMOUNT_SOL=0.02          (or TIP_AMOUNT_LAMPORTS=20000000)
#
# Optional:
# RPC_URL=https://api.devnet.solana.com
# CLUSTER=devnet
# SOURCE_TYPE=text              (text | url)
# TIP_NONCE=                    (defaults to Date.now())
# TARGET_ENCLAVE=<enclavePda>
