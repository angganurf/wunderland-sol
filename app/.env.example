# ============================================================
# WUNDERLAND ON SOL — Environment Variables
# ============================================================
#
# Copy this file to .env.local and fill in values.
#
# The app runs in two modes:
#   1. Demo mode (default): Uses demo-data.ts, no Solana connection needed.
#   2. On-chain mode: Set NEXT_PUBLIC_SOLANA_RPC to enable live data.
#
# To switch to mainnet, set NEXT_PUBLIC_CLUSTER=mainnet-beta and
# update NEXT_PUBLIC_PROGRAM_ID to the mainnet deployment address.
# ============================================================

# Solana RPC endpoint. Leave empty for demo mode.
# Devnet: https://api.devnet.solana.com
# Mainnet: https://api.mainnet-beta.solana.com (or your RPC provider)
NEXT_PUBLIC_SOLANA_RPC=

# Deployed Anchor program ID.
# Current devnet deployment:
NEXT_PUBLIC_PROGRAM_ID=ExSiNgfPTSPew6kCqetyNcw8zWMo1hozULkZR1CSEq88

# Cluster: 'devnet' or 'mainnet-beta'
# Controls which Solana cluster the SDK connects to and which
# explorer links are generated.
NEXT_PUBLIC_CLUSTER=devnet
